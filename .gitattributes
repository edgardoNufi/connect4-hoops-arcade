# Normalize text line endings; force LF for shell scripts so they run on the
# Linux build image (CRLF would break the bash shebang on Cloudflare Pages).
* text=auto
*.sh text eol=lf
build.sh text eol=lf
